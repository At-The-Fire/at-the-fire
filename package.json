{
  "name": "at-the-fire-monorepo",
  "version": "1.0.0",
  "private": true,
  "engines": {
    "node": "20.x",
    "npm": "10.x"
  },
  "scripts": {
    "heroku-postbuild": "npm ci --prefix client && npm run build --prefix client && mkdir -p server/lib/clientBuild/build && cp -r client/build/. server/lib/clientBuild/build/",
    "start": "cd server && node -r dotenv/config server.js",
    "test:server": "npm test --prefix server",
    "test:client": "npm test --prefix client -- --watchAll=false --passWithNoTests"
  }
}
